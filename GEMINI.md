# IRONSIGHT

**Read `CLAUDE.md` — it is the single source of truth for this project.**

It covers the stack, commands, project structure, the graphify knowledge graph, and the
working agreements (rigor tiers, gates, `[SPEC]` format, commit conventions).

The architectural roadmap lives in `draft-implementation-plan.md`.

This file used to duplicate `CLAUDE.md` verbatim. It no longer does, so that the two cannot
drift apart. Put project instructions in `CLAUDE.md` only.
